import { DynamicviewrefDirective } from './dynamicviewref.directive';

describe('DynamicviewrefDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicviewrefDirective();
    expect(directive).toBeTruthy();
  });
});
